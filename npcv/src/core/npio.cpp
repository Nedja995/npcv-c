#include "npio.h"

#include "stdio.h"
#include <stdarg.h>

namespace npcore
{
	//void sprintfN(char * dst, const char * formating, ...)
	//{
	//		int ret;
	//		va_list args;


	//		va_start(args, formating);
	//		sprintf(dst, formating, args);
	//	//		ret = do_xprintf(outputfunc, NULL, fmt, args);
	//		va_end(args);
	//	//	return ret;
	//}
	/*void sprintfN(char * dst, const char * formating, char * arg, int arg2)
	{
		std::sprintf(dst, formating, arg);
	}*/
}